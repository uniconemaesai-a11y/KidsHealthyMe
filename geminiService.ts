
import { GoogleGenAI } from "@google/genai";
import { HealthLog } from "../types";

const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });

/**
 * วิเคราะห์ข้อมูลภาพรวมชั้นเรียนสำหรับคุณครู
 */
export async function getClassReport(allLogs: HealthLog[], studentCount: number) {
  try {
    const recentLogs = allLogs.slice(-100);
    
    const prompt = `
      คุณคือ "ผู้เชี่ยวชาญด้านสุขภาวะโรงเรียน" (School Wellness Expert)
      ข้อมูลภาพรวมชั้นเรียน:
      - จำนวนนักเรียนทั้งหมด: ${studentCount} คน
      - จำนวนบันทึกสุขภาพล่าสุด: ${recentLogs.length} รายการ
      - ข้อมูลดิบ (JSON): ${JSON.stringify(recentLogs.map(l => ({ 
          date: l.date, 
          steps: l.steps, 
          sleep: l.sleep_hours, 
          mood: l.mood, 
          sickness: l.sickness 
        })))}

      งานของคุณคือ:
      1. สรุปภาพรวมสุขภาพของห้องเรียนนี้ (1 ประโยค)
      2. ระบุ "จุดแข็ง" และ "จุดที่ต้องกังวล"
      3. ตรวจสอบว่ามีสัญญาณของความไม่สบายที่ผิดปกติหรือไม่
      4. แนะนำกิจกรรม 1 อย่างที่คุณครูควรจัดในสัปดาห์หน้าเพื่อส่งเสริมสุขภาพ

      ใช้ภาษาไทยที่เป็นทางการแต่เข้าใจง่าย สรุปเป็นข้อๆ พร้อมอีโมจิที่เหมาะสม
    `;

    const response = await ai.models.generateContent({
      model: 'gemini-3-flash-preview',
      contents: prompt,
      config: {
        systemInstruction: "คุณคือที่ปรึกษาด้านสุขภาพและจิตวิทยาเด็กสำหรับคุณครูประถมศึกษา",
        temperature: 0.7,
      },
    });

    return response.text || "ไม่สามารถวิเคราะห์ข้อมูลได้ในขณะนี้";
  } catch (error) {
    console.error("Gemini Admin Error:", error);
    return "เกิดข้อผิดพลาดในการวิเคราะห์ข้อมูลชั้นเรียน 🤖";
  }
}

/**
 * ให้ Gemini ช่วยสร้างข้อสอบชุดใหญ่ตามหัวข้อ
 */
export async function generateQuizQuestions(topic: string, count: number = 5) {
  try {
    const prompt = `สร้างคำถามควิซเกี่ยวกับหัวข้อ "${topic}" สำหรับเด็กประถมศึกษาจำนวน ${count} ข้อ 
    โดยให้ผลลัพธ์อยู่ในรูปแบบ Text (Pipe-separated) ดังนี้เท่านั้น (ห้ามมี Markdown Code Blocks):
    คำถาม | ตัวเลือกที่ 1 | ตัวเลือกที่ 2 | ตัวเลือกที่ 3 | ตัวเลือกที่ 4 | ลำดับคำตอบที่ถูกต้อง(0-3) | อีโมจิที่เกี่ยวข้องกับคำถาม
    
    กฎการสร้าง:
    1. ภาษาต้องเข้าใจง่ายสำหรับเด็ก (อายุ 7-12 ปี)
    2. ตัวเลือกต้องไม่ซับซ้อนเกินไป
    3. คำตอบต้องถูกต้องตามหลักการแพทย์และสุขอนามัย
    4. อีโมจิห้ามว่าง
    5. ส่งกลับมาเฉพาะบรรทัดข้อมูลดิบเท่านั้น ห้ามมีหัวข้อ ห้ามมีเครื่องหมาย \`\`\` หรือคำเกริ่นใดๆ
    
    ตัวอย่าง:
    การล้างมือที่ถูกวิธีช่วยป้องกันอะไร? | ป้องกันหวัด | ป้องกันผมร่วง | ป้องกันหูอื้อ | ป้องกันเจ็บเข่า | 0 | 🧼`;

    const response = await ai.models.generateContent({
      model: 'gemini-3-flash-preview',
      contents: prompt,
      config: {
        systemInstruction: "คุณคือครูผู้เชี่ยวชาญการสร้างข้อสอบวิชาสุขศึกษาสำหรับเด็กประถม ให้ส่งเฉพาะข้อมูลดิบในรูปแบบที่กำหนดเท่านั้น",
        temperature: 0.8,
      },
    });

    let text = response.text || "";
    // Clean up markdown if AI includes it
    text = text.replace(/```[a-z]*\n?/gi, '').replace(/```/g, '').trim();
    
    return text;
  } catch (error) {
    console.error("Generate Quiz Error:", error);
    throw new Error("AI ไม่สามารถสร้างคำถามได้ในขณะนี้");
  }
}

/**
 * ฟังก์ชันเดิมสำหรับ AI Coach
 */
export async function getAICoachFeedback(
  currentLog: HealthLog, 
  history: HealthLog[], 
  level: number, 
  username: string,
  isParentView: boolean = false
) {
  try {
    const historicalContext = history.slice(-7).map(h => ({
      date: h.date,
      steps: h.steps,
      mood: h.mood,
      veggie: h.vegetable_score,
      exercise: h.exercise_minutes
    }));

    const avgSteps = history.length > 0 
      ? history.reduce((acc, curr) => acc + (Number(curr.steps) || 0), 0) / history.length 
      : 0;
    
    const loginStreak = history.length;

    const systemInstruction = isParentView 
      ? `คุณคือ "ที่ปรึกษาครอบครัว AI" ผู้เชี่ยวชาญด้านสุขภาพเด็ก ให้ข้อมูลเชิงวิเคราะห์และคำแนะนำแก่ผู้ปกครองอย่างสุภาพและจริงใจ`
      : `คุณคือ "พี่หมอหุ่นยนต์" ผู้เชี่ยวชาญด้านสุขภาพสำหรับเด็กประถมศึกษา ใจดี ขี้เล่น และชอบให้กำลังใจ`;

    const prompt = `
      ข้อมูลของเด็กชื่อ: ${username}
      สถานะฮีโร่: Level ${level}
      บันทึกสะสมมาแล้ว: ${loginStreak} วัน
      ค่าเฉลี่ยก้าวเดินที่ผ่านมา: ${avgSteps.toFixed(0)} ก้าว
      
      ข้อมูลวันนี้:
      - ก้าวเดิน: ${currentLog.steps}
      - ออกกำลังกาย: ${currentLog.exercise_minutes} นาที
      - กินผัก: คะแนน ${currentLog.vegetable_score}/10
      - การนอน: ${currentLog.sleep_hours} ชม.
      - อารมณ์: ${currentLog.mood}

      ประวัติย้อนหลังสั้นๆ: ${JSON.stringify(historicalContext)}

      งานของคุณคือ:
      1. ${isParentView ? 'สรุปภาพรวมสุขภาพและแนวโน้มให้ผู้ปกครองฟัง' : 'ให้คำชมที่เจาะจงกับสิ่งที่ทำได้ดีในวันนี้'}
      2. วิเคราะห์ความสม่ำเสมอ
      3. วิเคราะห์แนวโน้ม
      4. เชื่อมโยงกับ Level
      5. ${isParentView ? 'แนะนำกิจกรรมที่ผู้ปกครองควรทำร่วมกับลูก' : 'ให้ 1 ทิปลับสำหรับภารกิจพรุ่งนี้'}

      ใช้ภาษาไทยที่เป็นมิตร 3-4 ประโยค พร้อมอีโมจิ
    `;

    const response = await ai.models.generateContent({
      model: 'gemini-3-flash-preview',
      contents: prompt,
      config: {
        systemInstruction: systemInstruction,
        temperature: 0.8,
      },
    });

    return response.text || "ว้าว! วันนี้เก่งมากเลยครับ มาพยายามต่อในวันพรุ่งนี้นะ! 🌟";
  } catch (error) {
    return "วันนี้ทำได้ดีมากเลยเด็กๆ! ✨";
  }
}
